_build/
needs*.json
needs.parquet
.ub_cache/
# generated by the ubtrace-prepare script — must never be committed (KB-scale
# push requirement, R14): the >10MB D18 gate asset and the fetched dependency
# stub are both produced fresh by the build pipeline's prepare step.
_static/big.bin
/fetched-dep.json
